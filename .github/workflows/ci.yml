name: CI

on:
  push:
  pull_request:

# Prevent overlapping runs on the same branch
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  checks: write
  pull-requests: write
  actions: read

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    permissions:
      contents: read

    env:
      NODE_ENV: production
      FORCE_COLOR: 1

    steps:
      # ---------------------------------------------------------
      # 0. Pre‑firewall environment hygiene
      # ---------------------------------------------------------
      - name: 🛡️ Pre-firewall environment setup
        run: |
          npm config set audit false
          npm config set fund false
          npm config set update-notifier false

      # ---------------------------------------------------------
      # 1. Checkout
      # ---------------------------------------------------------
      - name: ⬇️ Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # ---------------------------------------------------------
      # 2. Node setup
      # ---------------------------------------------------------
      - name: 🧰 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # ---------------------------------------------------------
      # 3. Install dependencies
      # ---------------------------------------------------------
      - name: 📦 Install dependencies
        run: npm ci

      # ---------------------------------------------------------
      # 4. Repo Health (optional hook)
      # ---------------------------------------------------------
      - name: 🩺 Repo Health Scan
        run: |
          if [ -f ./tools/repo-health/scan.sh ]; then
            chmod +x ./tools/repo-health/scan.sh
            ./tools/repo-health/scan.sh
          else
            echo "No repo-health tool found. Skipping."
          fi

      # ---------------------------------------------------------
      # 5. Bootstrap (environment validation + build prep)
      # ---------------------------------------------------------
      - name: 🚀 Run Bootstrap
        run: |
          chmod +x tools/bootstrap/bootstrap.sh
          ./tools/bootstrap/bootstrap.sh

      # ---------------------------------------------------------
      # 6. Audit (contract + workflow validation)
      # ---------------------------------------------------------
      - name: 🔍 Run Audit
        run: |
          chmod +x tools/audit/audit.sh
          ./tools/audit/audit.sh

      # ---------------------------------------------------------
      # 7. Lint
      # ---------------------------------------------------------
      - name: 🧹 Lint
        run: npm run lint

      # ---------------------------------------------------------
      # 8. Typecheck
      # ---------------------------------------------------------
      - name: 🧠 Typecheck
        run: npm run typecheck

      # ---------------------------------------------------------
      # 9. Tests
      # ---------------------------------------------------------
      - name: 🧪 Run tests
        run: npm test

      # ---------------------------------------------------------
      # 10. Build
      # ---------------------------------------------------------
      - name: 🏗️ Build
        run: npm run build

      # ---------------------------------------------------------
      # 11. Upload build artifacts (operator observability)
      # ---------------------------------------------------------
      - name: 📦 Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: |
            dist
            site/dist
          if-no-files-found: ignore

      # ---------------------------------------------------------
      # 12. Slack Notification
      # ---------------------------------------------------------
      - name: 🔔 Notify Slack
        if: always()
        uses: slackapi/slack-github-action@v1.24.0
        with:
          payload: |
            {
              "text": "⚙️ *CyberAi Operator CI* on `${{ github.ref_name }}`:\n*Status:* ${{ job.status }}\n*Commit:* <${{ github.event.head_commit.url }}|${{ github.event.head_commit.message }}>\n*Author:* ${{ github.event.head_commit.author.name }}"
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      # ---------------------------------------------------------
      # 13. Discord Notification
      # ---------------------------------------------------------
      - name: 🔔 Notify Discord
        if: always()
        run: |
          curl -H "Content-Type: application/json" \
               -X POST \
               -d "{\"content\":\"⚙️ *CyberAi Operator CI* on \`${{ github.ref_name }}\` — *${{ job.status }}*\"}" \
               ${{ secrets.DISCORD_WEBHOOK_URL }}
